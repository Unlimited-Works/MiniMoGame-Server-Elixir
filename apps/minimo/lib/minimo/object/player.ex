defmodule Minimo.Object.Role.Player do
  @doc ~S"""
  Player module service player object at backend, such as player positions, 
  """
  
  def get_pos(str_id, {x, y, z}) do
    
  end

  def set_pos(str_id, {x, y, z}) do

  end
  
  
end

